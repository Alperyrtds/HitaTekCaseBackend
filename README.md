# HitaTekCaseBackend

Hata olursa HitaTekCase2 içinde startup.cs de 47. satır app.UseCors(builder => builder.WithOrigins("http://localhost:3000").AllowAnyHeader()); local hostu kendi react projenizinkiyle değiştirin lütfen. 
